# surviveJPSH 迁移计划 - 2026-08-25

## 目标

将当前纯静态 HTML 网站重构为 **Vue 3 + Vite** 项目，集成 WinUI on Web 组件库，实现：
- WinUI 风格的现代化 UI（侧边栏导航、面包屑、暗色模式等）
- Markdown 文档动态渲染
- 同时兼容 **Cloudflare Workers** 和 **GitHub Pages** 部署

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | ^3.5.32 | UI 框架 |
| Vue Router | ^4.6.4 | 客户端路由 |
| Vite | ^8.0.8 | 构建工具 |
| TypeScript | ^6.0.0 | 类型安全 |
| markdown-it | ^14.0.0 | Markdown 渲染 |
| WinUI on Web | (复制核心组件) | UI 组件库 |

## 决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-08-25 | `base` 路径通过环境变量 `BASE_PATH` 动态设置 | GitHub Pages 需要 `/surviveJPSH/`，Cloudflare 需要 `/` |
| 2026-08-25 | 开发阶段暂不处理许可证变更 | WinUIonWeb GPL-3.0 vs 当前 CC BY-SA 4.0 |
| 2026-08-25 | 需要搜索功能 | 使用 WinAutoSuggestBox + 搜索索引 |
| 2026-08-25 | 需要关于页 | 展示项目信息和许可证 |

---

## 项目结构

```
surviveJPSH/
├── .github/workflows/deploy.yml    # GitHub Pages 部署
├── wrangler.jsonc                   # Cloudflare Workers 部署
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── index.html                       # Vite 入口 HTML
├── src/
│   ├── main.ts                      # 应用入口
│   ├── App.vue                      # WinNavigationView + router-view
│   ├── router.ts                    # 路由配置
│   ├── styles/
│   │   ├── theme.css                # WinUI 主题设计系统（1040行）
│   │   ├── animations.css           # 导航过渡动画（805行）
│   │   └── docs.css                 # 文档展示自定义样式
│   ├── components/                  # WinUI 核心组件
│   │   ├── WinNavigationView.vue    # 侧边栏导航
│   │   ├── WinScrollViewer.vue      # 内容滚动容器
│   │   ├── WinBreadcrumbBar.vue     # 面包屑导航
│   │   ├── WinTitleBar.vue          # 标题栏
│   │   ├── WinTextBlock.vue         # 文本显示
│   │   ├── WinButton.vue            # 按钮
│   │   ├── WinExpander.vue          # 可折叠章节
│   │   ├── WinAutoSuggestBox.vue    # 搜索框
│   │   ├── WinInfoBadge.vue         # 信息徽章
│   │   ├── WinMenuFlyout.vue        # 下拉菜单
│   │   ├── WinStackPanel.vue        # 布局容器
│   │   ├── WinGrid.vue              # 网格布局
│   │   ├── WinToolTipService.vue    # 工具提示
│   │   ├── useFlyoutAnimation.ts    # 动画逻辑
│   │   └── i18n/
│   │       ├── index.ts
│   │       └── Strings/
│   │           ├── en-US/Resources.ts
│   │           └── zh-CN/Resources.ts
│   ├── views/
│   │   ├── HomeView.vue             # 首页
│   │   ├── DocView.vue              # Markdown 文档渲染页
│   │   └── AboutView.vue            # 关于页
│   ├── docs/                        # Markdown 源文件
│   │   ├── study.md
│   │   └── life.md
│   ├── utils/
│   │   ├── markdown.ts              # markdown-it 渲染器
│   │   ├── searchIndex.ts           # 搜索索引
│   │   └── navigationTransitionInfo.js
│   └── assets/
│       └── Fonts/
│           └── SEGOEICONS.TTF       # 图标字体
├── public/
│   └── favicon.ico
├── raw/                             # 原始 docx 源文件（保留）
│   ├── 学习篇.docx
│   ├── 学习篇.md
│   ├── 生活篇.docx
│   └── 生活篇.md
└── docs/                            # 旧 HTML 文件（阶段6删除）
```

---

## 阶段 1：项目初始化

### 1.1 package.json

```json
{
  "name": "survivejpsh",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy:cf": "npm run build && wrangler deploy",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.32",
    "vue-router": "^4.6.4",
    "markdown-it": "^14.0.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.0.0",
    "@vitejs/plugin-vue": "^6.0.6",
    "typescript": "^6.0.0",
    "vite": "^8.0.8",
    "vue-tsc": "^3.2.6",
    "wrangler": "^4.118.0"
  }
}
```

### 1.2 vite.config.ts

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  base: process.env.BASE_PATH || '/',
  plugins: [vue()],
  server: { port: 3000 }
})
```

### 1.3 wrangler.jsonc

更新 assets 指向 dist：
```jsonc
{
  "name": "survivejpsh",
  "compatibility_date": "2026-08-05",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
```

---

## 阶段 2：复制 WinUIonWeb 核心文件

从 `E:\WorkSpace\WinUIonWeb\WinUIonWeb\src\` 复制：

| 源文件 | 目标位置 |
|--------|----------|
| `styles/theme.css` | `src/styles/theme.css` |
| `styles/animations.css` | `src/styles/animations.css` |
| `components/WinNavigationView.vue` | `src/components/` |
| `components/WinScrollViewer.vue` | `src/components/` |
| `components/WinBreadcrumbBar.vue` | `src/components/` |
| `components/WinTitleBar.vue` | `src/components/` |
| `components/WinTextBlock.vue` | `src/components/` |
| `components/WinButton.vue` | `src/components/` |
| `components/WinExpander.vue` | `src/components/` |
| `components/WinAutoSuggestBox.vue` | `src/components/` |
| `components/WinInfoBadge.vue` | `src/components/` |
| `components/WinMenuFlyout.vue` | `src/components/` |
| `components/WinStackPanel.vue` | `src/components/` |
| `components/WinGrid.vue` | `src/components/` |
| `components/WinToolTipService.vue` | `src/components/` |
| `components/useFlyoutAnimation.ts` | `src/components/` |
| `components/i18n/` | `src/components/i18n/` |
| `assets/Fonts/SEGOEICONS.TTF` | `src/assets/Fonts/` |
| `utils/navigationTransitionInfo.js` | `src/utils/` |

**注意**：复制后需要检查组件中的 import 路径，确保路径正确。

---

## 阶段 3：内容迁移

1. 复制 `raw/学习篇.md` → `src/docs/study.md`
2. 复制 `raw/生活篇.md` → `src/docs/life.md`
3. 清理 Markdown 中的 `&emsp;` HTML 实体
4. 创建文档索引元数据

---

## 阶段 4：开发 Vue 组件

### 4.1 路由配置 (router.ts)

```
/           → HomeView.vue
/doc/:id    → DocView.vue (id = study | life)
/about      → AboutView.vue
```

### 4.2 搜索功能

- 基于文档标题和内容生成搜索索引
- 支持中文字符匹配
- WinAutoSuggestBox 输入时实时过滤

### 4.3 文档渲染

- 使用 markdown-it 渲染 Markdown
- 支持扩展插件：anchor（标题锚点）、highlight.js（代码高亮）
- WinScrollViewer 包裹内容区域

---

## 阶段 5：部署配置

### 5.1 Cloudflare Workers

```jsonc
{
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
```

部署命令：`npm run build && npx wrangler deploy`

### 5.2 GitHub Actions

```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: ['main']
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: BASE_PATH=/surviveJPSH/ npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
      - uses: actions/deploy-pages@v4
```

---

## 阶段 6：清理

1. 删除 `docs/` 目录下旧的 HTML 文件
2. 保留 `raw/` 目录作为源文件备份
3. 更新 `README.md`

---

## 文件变更清单

| 操作 | 文件 |
|------|------|
| 新建 | package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html |
| 新建 | src/main.ts, src/App.vue, src/router.ts |
| 新建 | src/styles/theme.css, src/styles/animations.css, src/styles/docs.css |
| 新建 | src/views/HomeView.vue, src/views/DocView.vue, src/views/AboutView.vue |
| 新建 | src/utils/markdown.ts, src/utils/navigationTransitionInfo.js, src/utils/searchIndex.ts |
| 新建 | src/docs/study.md, src/docs/life.md |
| 新建 | src/assets/Fonts/SEGOEICONS.TTF |
| 新建 | 12 个 WinUI 组件 + i18n 目录 |
| 新建 | .github/workflows/deploy.yml |
| 修改 | wrangler.jsonc（assets 指向 dist） |
| 修改 | README.md |
| 删除 | docs/index.html, docs/pages/, docs/static/ |

---

## 实施顺序

1. 项目初始化 + 依赖安装
2. 复制 WinUIonWeb 核心文件
3. 创建路由 + App.vue 主布局
4. 开发 HomeView（首页）
5. 开发 DocView（文档渲染）
6. 开发 AboutView
7. 实现搜索功能
8. 部署配置（Wrangler + GitHub Actions）
9. 清理旧文件
10. 测试 + 修复

**预计总工作量：约 4-5 小时**

---

*计划创建时间: 2026-08-25*
